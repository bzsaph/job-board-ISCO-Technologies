{
  "name": "job-board-backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "node src/db.js migrate",
    "migrate:rollback": "node src/db.js rollback",
    "seed": "node src/seeds/seed.js",
    "test": "jest --runInBand",
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist/"
    
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.0",
    "knex": "^3.1.0",
    "multer": "^2.0.2",
    "react-icons": "^5.5.0",
    "some-module": "^0.0.3",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^2.0.22",
    "supertest": "^6.3.3"
  }
}
